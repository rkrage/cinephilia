<h1>News Feed</h1>
<%= will_paginate @recentActivity%>
	<% @recentActivity.each do |data| %>
	<p>
		<%=data['user']%> added <a href="/movies/<%= data['imdbid']%>"><%= data['title']%></a> to their <%=data['list']%> list.
		<br/> &nbsp;-<%=data['created_at'].strftime('%m/%d/%y, %l:%M %p')%>
	</p>
	<% end %>
</ol>
<%= will_paginate @recentActivity%> 
